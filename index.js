function getCredentials(getUsername, getPassword) {
    var username = document.getElementById(getUsername).value;
    //alert("Value inside txt box: " + str);
    var password = document.getElementById(getPassword).value;
}

document.write(getCredentials().password);
var json = { "combo": [{}] }


